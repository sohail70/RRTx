  global_frame: /map
  robot_base_frame: /base_link
  transform_tolerance: 0.2
  robot_radius: 0.05

  obstacle_range: 3.5
  raytrace_range: 3.5

  update_frequency: 5.0
  publish_frequency: 2.0
  rolling_window: true
  static_map: true
  width: 22
  height: 22
  resolution: 0.1 #heavy load!
  origin_x: -11
  origin_y: -11
 # in plugin ro ghat kunam oza behtar mishe !
  #plugins:
   # - {name: static_map, type: "costmap_2d::StaticLayer"}
   # - {name: inflation, type: "costmap_2d::InflationLayer"}
   # - {name: obstacles,        type: "costmap_2d::VoxelLayer"}
  static:
    unknown_cost_value: -1
    lethal_cost_threshold: 100
    map_topic: /map

  inflation:
    inflation_radius: 0.1
    cost_scaling_factor: 0.2
  #obstacles:
  observation_sources: laser_scan_sensor
  laser_scan_sensor: {sensor_frame: base_laser_link, data_type: LaserScan, topic: /base_scan, marking: true, clearing: true, max_obstacle_height: 20.0, min_obstacle_height: 0.0}

